# Contributing

Issues and pull requests are welcome.

Before opening a pull request:

1. Build with `swift build -c release`.
2. Run `swift test` with a full Xcode toolchain.
3. Keep the Atoll card within one page at the default expanded height.
4. Do not commit credentials, account responses or private usage data.

For layout changes, attach a current Atoll screenshot to the pull request.
